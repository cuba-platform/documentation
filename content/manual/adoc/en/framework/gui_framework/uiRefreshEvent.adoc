:sourcesdir: ../../../../source

[[uiRefreshEvent]]
==== Web Page Refresh Event

`UIRefreshEvent` is an application event sent on each web page refresh. It's a <<ui_events,UiEvent>>, so it can be handled only by event listener methods of screen controllers.

[source, java]
----
include::{sourcesdir}/gui/uiRefreshEvent.java[]
----


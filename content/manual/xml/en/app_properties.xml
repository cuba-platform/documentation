<?xml version='1.0' encoding='UTF-8'?>
<!DOCTYPE appendix PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN" "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd">
<appendix id="app_properties_reference">
  <title>Application Properties</title>
  <para>This appendix describes all available <link linkend="app_properties">application properties</link> in alphabetical order.</para>
  <variablelist>
    <varlistentry id="cuba.allowQueryFromSelected">
      <term>cuba.allowQueryFromSelected</term>
      <listitem>
        <para>Allows the <link linkend="gui_Filter">generic filter</link> to use sequential filtering mode. See also  <xref linkend="query_from_selected"/>.</para>
        <para>Default value:  <code>true</code></para>
        <para>Interface: <code>GlobalConfig</code></para>
        <para>Used in the Web Client and the Middleware blocks.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.allowSetNotLoadedAttributes">
      <term>cuba.allowSetNotLoadedAttributes</term>
      <listitem>
        <para>Allows invoking setters for unloaded attributes of <link linkend="entity_states">Detached</link> entities. For more information, see <xref linkend="views"/>.</para>
        <para>Default value: <code>false</code></para>
        <para>Used in all application blocks.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.automaticDatabaseUpdate">
      <term>cuba.automaticDatabaseUpdate</term>
      <listitem>
        <para>Determines whether server should run <link linkend="db_update_server">DB update scripts</link> at application start.</para>
        <para>Default value: <code>false</code></para>
        <para>Interface: <code>ServerConfig</code></para>
        <para>Used in the Middleware block.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.availableLocales">
      <term>cuba.availableLocales</term>
      <listitem>
        <para id="property_available_locales">List of supported user interface languages. </para>
        <para id="property_available_locales_1">Property format: <literal>{language_name1}|{language_code_1};{language_name2}|{language_code_2};</literal>...</para>
        <para>Example: <programlisting>cuba.availableLocales=French|fr;English|en</programlisting></para>
        <para><code>{language_name}</code> – name displayed in the list of available languages. For example, such lists can be found on the login screen and on the user edit screen. </para>
        <para><code>{language_code}</code> – corresponds to language code returned by the <code>Locale.getLanguage()</code> method. Used as a suffix for <link linkend="message_packs">message pack</link> file names. For example, <code>messages_fr.properties</code>.</para>
        <para>The first language listed in the <property>cuba.availableLocales</property> property, will be selected in the list of available languages by default, if the list does not contain the user’s operating system language. Otherwise, user’s operating system language will be selected by default. </para>
        <para>Default value: <literal>English|en;Russian|ru;French|fr</literal></para>
        <para>Interface: <code>GlobalConfig</code></para>
        <para>Used in all standard <link linkend="app_tiers">blocks</link>.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.backgroundWorker.maxActiveTasksCount">
      <term>cuba.backgroundWorker.maxActiveTasksCount</term>
      <listitem>
        <para>The maximum number of active <link linkend="background_tasks">background tasks</link>.</para>
        <para>Default value: <literal>100</literal></para>
        <para>Interface: <code>WebConfig</code></para>
        <para>Used in the Web Client block.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.backgroundWorker.maxClientLatencySeconds">
      <term>cuba.backgroundWorker.maxClientLatencySeconds</term>
      <listitem>
        <para>Delay in seconds added to the timeout of the <link linkend="background_tasks">background task</link>, after which the task is interrupted by the <code>WatchDog</code> mechanism. This delay compensates for the possible network latencies while polling task status.</para>
        <para>Default value: <literal>60</literal></para>
        <para>Interface: <code>WebConfig</code></para>
        <para>Used in the Web Client block.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.backgroundWorker.uiCheckInterval">
      <term>cuba.backgroundWorker.uiCheckInterval</term>
      <listitem>
        <para><link linkend="background_tasks">Background task</link> status polling interval.</para>
        <para>Default value: <literal>2000</literal></para>
        <para>Interface: <code>WebConfig</code></para>
        <para>Used in the Web Client block.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.client.maxUploadSizeMb">
      <term>cuba.client.maxUploadSizeMb</term>
      <listitem>
        <para>Maximum file size (in megabytes) that can be uploaded using the <link linkend="gui_FileUploadField">FileUploadField</link> and <link linkend="gui_FileMultiUploadField">FileMiltiUploadField</link> components.</para>
        <para>Default value: <literal>20</literal></para>
        <para>Interface: <code>ClientConfig</code></para>
        <para>Used in the Web Client and the Desktop Client blocks.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.confDir">
      <term>cuba.confDir</term>
      <listitem>
        <para>Defines location of the <link linkend="conf_dir">configuration folder</link> for an application <link linkend="app_tiers">block</link>.</para>
        <para>Default value for the Middleware, Web Client and Web Portal blocks: <literal>${catalina.home}/conf/${<link linkend="cuba.webContextName">cuba.webContextName</link>}</literal></para>
        <para>In case of a standard <application>Tomcat</application> deployment, this points to a sub-folder with the name of the current web app in the <filename>tomcat/conf</filename> folder, for example, <filename>tomcat/conf/app-core</filename>.</para>
        <para>Default value for the Desktop Client block: <literal>${<link linkend="cuba.desktop.home">cuba.desktop.home</link>}/conf</literal>. </para>
        <para>Interface: <code>GlobalConfig</code></para>
        <para>Used in all standard <link linkend="app_tiers">blocks</link>.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.cluster.enabled">
      <term>cuba.cluster.enabled</term>
      <listitem>
        <para>Enables interaction between Middleware servers in a cluster. See <xref linkend="cluster_mw_server"/> for details.</para>
        <para>Default value: <code>false</code></para>
        <para>Used in the Middleware block.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.connectionUrlList">
      <term>cuba.connectionUrlList</term>
      <listitem>
        <para>Sets Middleware server connection URL for <link linkend="app_tiers">client blocks</link>.</para>
        <para>Property value should contain one or more comma separated URLs <code>http[s]://host[:port]/app-core</code>, where <code>host</code> is the server hostname, <code>port</code> is the server port, and <code>app-core</code> is the name of the the Middleware block web application. For example:<programlisting>cuba.connectionUrlList = http://localhost:8080/app-core</programlisting></para>
        <para>When using a cluster of Middleware servers, their addresses should be listed, separated with commas:<programlisting>cuba.connectionUrlList = http://server1:8080/app-core,http://server2:8080/app-core</programlisting></para>
        <para>The order of servers in the list, determines the order in which the client will attempt to send requests. In the example above, the client will send request to <code>server1</code> first, and if it is inaccessible, to <code>server2</code>. If a request to <code>server2</code> completes successfully, the client will use <code>server2</code> as the primary server and will continue working with it. Restarting the client will reset the list to the initial value. Uniform distribution of clients across a cluster of servers can be achieved by using the <link linkend="cuba.randomServerPriority">cuba.randomServerPriority</link> property.</para>
        <para>See also <link linkend="cuba.useLocalServiceInvocation">cuba.useLocalServiceInvocation</link>.</para>
        <para>Interface: <code>ClientConfig</code></para>
        <para>Used in the Web Client, Web Portal and Desktop Client <link linkend="app_tiers">blocks</link>.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.creditsConfig">
      <term>cuba.creditsConfig</term>
      <listitem>
        <para>Defines a set of <filename>credits.xml</filename> files containing <link linkend="credits">information about the software components</link> used by the application. </para>
        <para>Property value should contain a list of file names separated with spaces.  <link linkend="resources"> Resources</link> interface rules apply to loading files.</para>
        <para>Used in the Web Client and the Desktop Client blocks.</para>
        <para>Example:<programlisting>cuba.creditsConfig = cuba-credits.xml reports-credits.xml credits.xml</programlisting></para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.dataSourceJndiName">
      <term>cuba.dataSourceJndiName</term>
      <listitem>
        <para>Defines JNDI name of the <code>javax.sql.DataSource</code> object used for connection to the application database.</para>
        <para>Default value: <literal>java:comp/env/jdbc/CubaDS</literal></para>
        <para>Used in the Middleware block.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.dataDir">
      <term>cuba.dataDir</term>
      <listitem>
        <para>Defines the location of the <link linkend="work_dir">work folder</link> for an <link linkend="app_tiers">application block</link>.</para>
        <para>Default value for the Middleware, Web Client and Web Portal blocks: <literal>${catalina.home}/work/${<link linkend="cuba.webContextName">cuba.webContextName</link>}</literal></para>
        <para>In case of a standard <application>Tomcat</application> deployment, this points to a sub-folder with the name of the current web-app inside the <filename>tomcat/work</filename> folder, for example, <filename>tomcat/work/app-core</filename>.</para>
        <para>Default value for the Desktop Client block:  <literal>${<link linkend="cuba.desktop.home">cuba.desktop.home</link>}/work</literal>. </para>
        <para>Interface: <code>GlobalConfig</code></para>
        <para>Used in all standard <link linkend="app_tiers">blocks</link>.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.dbDir">
      <term>cuba.dbDir</term>
      <listitem>
        <para>Defines the location of the <link linkend="db_dir">database scripts folder</link>.</para>
        <para>Default value: <literal>${catalina.home}/webapps/${cuba.webContextName}/WEB-INF/db</literal>, which points to the <filename>WEB-INF/db</filename> sub-folder of the current web-app folder in <application>Tomcat</application>.</para>
        <para>Interface: <code>ServerConfig</code></para>
        <para>Used in the Middleware block.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.dbmsType">
      <term>cuba.dbmsType</term>
      <listitem>
        <para>Defines the DBMS type. Affects the choice of DBMS integration interface implementations and the search for database init and update scripts together with <link linkend="cuba.dbmsVersion">cuba.dbmsVersion</link>.</para>
        <para>See <xref linkend="dbms_types"/> for details.   </para>
        <para>Default value: <literal>hsql</literal></para>
        <para>Used in the Middleware block.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.dbmsVersion">
      <term>cuba.dbmsVersion</term>
      <listitem>
        <para>An optional property that sets the database version. Affects the choice of DBMS integration interface implementations and the search for database init and update scripts together with <link linkend="cuba.dbmsType">cuba.dbmsType</link>.</para>
        <para>See <xref linkend="dbms_types"/> for details.   </para>
        <para>Default value: <literal>none</literal></para>
        <para>Used in the Middleware block.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.defaultQueryTimeoutSec">
      <term>cuba.defaultQueryTimeoutSec</term>
      <listitem>
        <para>Defines default <link linkend="transaction_timeout">transaction timeout</link>.</para>
        <para>Default value: <literal>0</literal> (no timeout).</para>
        <para>Interface: <code>ServerConfig</code></para>
        <para>Used in the Middleware block.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.desktop.useServerTime">
      <term>cuba.desktop.useServerTime</term>
      <listitem>
        <para>Enables adjustment of the time provided by the <link linkend="timeSource">TimeSource</link> interface of the Desktop Client block.The time will approximately equal to the time of the Middleware the client is connected to.</para>
        <para>Default value: <literal>true</literal></para>
        <para>Interface: <code>DesktopConfig</code></para>
        <para>Used in the Desktop Client block.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.desktop.useServerTimeZone">
      <term>cuba.desktop.useServerTimeZone</term>
      <listitem>
        <para>Enables adjustment of the Desktop Client block’s JVM timezone to the timezone of the Middleware the client is connected to.</para>
        <para>Default value: <literal>true</literal></para>
        <para>Interface: <code>DesktopConfig</code></para>
        <para>Used in the Desktop Client block.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.disableOrmXmlGeneration">
      <term>cuba.disableOrmXmlGeneration</term>
      <listitem>
        <para>Prohibits automatic generation of the <filename>orm.xml</filename> file for <link linkend="extension">extended</link> entities. Allows creating and registering this file manually in the <literal>openjpa.MetaDataFactory</literal> parameter of the  <link linkend="persistence.xml">persistence.xml</link> file.</para>
        <para>Default value: <literal>false</literal> (<filename>orm.xml</filename> will be created automatically if any extended entity exist).</para>
        <para>Used in the Middleware block.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.dispatcherSpringContextConfig">
      <term>cuba.dispatcherSpringContextConfig</term>
      <listitem>
        <para>Defines a set of <link linkend="dispatcher-spring.xml">dispatcher-spring.xml</link> files in the client blocks.</para>
        <para>The value of the property should contain a list of file names separated with spaces.  <link linkend="resources">Resources</link> interface rules apply to loading files.</para>
        <para>Used in the Web Client and Web Portal blocks.</para>
        <para>Example:<programlisting>cuba.dispatcherSpringContextConfig = cuba-portal-dispatcher-spring.xml portal-dispatcher-spring.xml</programlisting></para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.download.directories">
      <term>cuba.download.directories</term>
      <listitem>
        <para>Defines a list of folders from which the Middleware files can be downloaded from via <code>com.haulmont.cuba.core.controllers.FileDownloadController</code>. For example, file downloading is utilized by the server log display mechanism found in the <guimenu>Administration</guimenu> &gt; <guimenu>Server Log</guimenu> web client screen.</para>
        <para>The folder list should be separated with semicolon.</para>
        <para>Default value: <literal>${cuba.tempDir};${cuba.logDir}</literal> (files can be downloaded from the <link linkend="temp_dir">temporary folder</link> and the <link linkend="log_dir">logs folder</link>).</para>
        <para>Used in the Middleware block.</para>
      </listitem>
    </varlistentry>
    <varlistentry>
      <term>cuba.email.*</term>
      <listitem>
        <para>Email sending parameters described in  <xref linkend="email_sending_properties"/>.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.fileStorageDir">
      <term>cuba.fileStorageDir</term>
      <listitem>
        <para>Defines file storage folder structure roots. For more information, see  <xref linkend="file_storage_impl"/>.</para>
        <para>Default value: <literal>null</literal></para>
        <para>Used in the Middleware block.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.gui.genericFilterChecking">
      <term>cuba.gui.genericFilterChecking</term>
      <listitem>
        <para>Influences the behavior of the <link linkend="gui_Filter">Filter</link> component. </para>
        <para>When set to <code>false</code>, enables users to apply a filter without specifying any parameters. </para>
        <para>Default value:  <literal>true</literal></para>
        <para>Interface: <code>ClientConfig</code></para>
        <para>Used in the Web Client and Desktop Client blocks.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.gui.genericFilterManualApplyRequired">
      <term>cuba.gui.genericFilterManualApplyRequired</term>
      <listitem>
        <para>Influences the behavior of the <link linkend="gui_Filter">Filter</link> component. </para>
        <para>When set to <code>true</code>, the screens containing filters will not load the corresponding datasources automatically, until user clicks the filter <guibutton>Apply</guibutton> button. </para>
        <para>The value of <property>cuba.gui.genericFilterManualApplyRequired</property> is ignored, when opening browser screens using an application or search folders, i.e. the filter is applied. The filter will not be applied, if the <code>applyDefault</code> value for a folder is explicitly set to <literal>false</literal>.</para>
        <para>Default value: <literal>false</literal> </para>
        <para>Interface: <code>ClientConfig</code></para>
        <para>Used in the Web Client and Desktop Client blocks.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.gui.genericFilterTreeConditionSelect">
      <term>cuba.gui.genericFilterTreeConditionSelect</term>
      <listitem>
        <para>Influences the behavior of the <link linkend="gui_Filter">Filter</link> component.</para>
        <para>When set to <code>false</code>, allows using a flat drop-down list instead of a tree structure for choosing conditions.</para>
        <para>Default value:  <literal>true</literal></para>
        <para>Interface: <code>ClientConfig</code></para>
        <para>Used in the Web Client and Desktop Client blocks.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.groovyEvaluationPoolMaxIdle">
      <term>cuba.groovyEvaluationPoolMaxIdle</term>
      <listitem>
        <para>Sets the maximum number of unused compiled Groovy expressions in the pool during   <code>Scripting.evaluateGroovy()</code> method execution. It is recommended to increment this parameter when intensive execution of Groovy expressions is required, for example, for a large number of  <link linkend="application_folder">application folders</link>. </para>
        <para>Default value:  8</para>
        <para>Used in all standard  <link linkend="app_tiers">blocks</link>.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.groovyEvaluatorImport">
      <term>cuba.groovyEvaluatorImport</term>
      <listitem>
        <para>Defines a list of classes imported by all Groovy expressions executed through  <link linkend="scripting">Scripting</link>.</para>
        <para>Class names in the list should be separated with commas or semicolons.</para>
        <para>Default value: <code>com.haulmont.cuba.core.global.PersistenceHelper</code></para>
        <para>Used in all standard <link linkend="app_tiers">blocks</link>.</para>
        <para>Example:<programlisting>cuba.groovyEvaluatorImport = com.haulmont.cuba.core.global.PersistenceHelper,com.abc.sales.CommonUtils</programlisting></para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.gui.useSaveConfirmation">
      <term>cuba.gui.useSaveConfirmation</term>
      <listitem>
        <para>Defines the layout of the dialog displayed when a user attempts closing a <link linkend="screens">screen</link> with unsaved changes in datasources.</para>
        <para>Value of <literal>true</literal> corresponds to a layout with three possible actions: <emphasis>Save changes</emphasis>, <emphasis>Don’t Save</emphasis>, <emphasis>Don’t close the screen</emphasis>.</para>
        <para>The value of <literal>false</literal> corresponds to a form with two options: <emphasis>Close the screen without saving changes</emphasis>, <emphasis>Don’t close the screen</emphasis>.</para>
        <para>Default value: <code>true</code></para>
        <para>Interface: <code>ClientConfig</code></para>
        <para>Used in the Web Client and Desktop Client blocks.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.httpSessionExpirationTimeoutSec">
      <term>cuba.httpSessionExpirationTimeoutSec</term>
      <listitem>
        <para>Defines HTTP-session inactivity timeout in seconds.</para>
        <para>Default value: <literal>1800</literal></para>
        <para>Interface: <code>WebConfig</code></para>
        <para>Used in the Web Client block.</para>
        <tip>
          <para>It is recommended to use the same values for <link linkend="cuba.userSessionExpirationTimeoutSec"> cuba.userSessionExpirationTimeoutSec</link> and <code>cuba.httpSessionExpirationTimeoutSec</code> properties.</para>
        </tip>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.inMemoryDistinct">
      <term>cuba.inMemoryDistinct</term>
      <listitem>
        <para>Enables in-memory filtering of duplicate records instead of using <literal>select distinct</literal> at the database level. Used by the <link linkend="dataManager">DataManager</link>.</para>
        <para>Default value: <code>false</code></para>
        <para>Interface: <code>ServerConfig</code></para>
        <para>Used in the Middleware block.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.jmxUserLogin">
      <term>cuba.jmxUserLogin</term>
      <listitem>
        <para>Defines a user login that should be used for <link linkend="system_authentication">system authentication</link>.</para>
        <para>Default value: <literal>admin</literal></para>
        <para>Used in the Middleware block.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.licensePath">
      <term>cuba.licensePath</term>
      <listitem>
        <para>Path to the CUBA platform license file. <link linkend="resources">Resources</link> interface rules apply to loading files. </para>
        <para>Default value: <literal>/cuba.license</literal></para>
        <para>Interface: <code>ServerConfig</code></para>
        <para>Used in the Middleware block.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.localeSelectVisible">
      <term>cuba.localeSelectVisible</term>
      <listitem>
        <para>Disables the user interface language selection when logging in.</para>
        <para>If <literal>cuba.localeSelectVisible</literal> is set to false, the locale for a user session is selected in the following way:<itemizedlist>
            <listitem>
              <para>If the <code>User</code> entity instance has a <code>language</code> attribute defined, the system will use this language.</para>
            </listitem>
            <listitem>
              <para>If the user’s operating system language is included in the list of available locales (set by the  <link linkend="cuba.availableLocales">cuba.availableLocales</link> property), the system will use this language.</para>
            </listitem>
            <listitem>
              <para>Otherwise, the system will use the first language defined in the  <link linkend="cuba.availableLocales">cuba.availableLocales</link> property.</para>
            </listitem>
          </itemizedlist></para>
        <para>Default value: <literal>true</literal></para>
        <para>Interface: <code>GlobalConfig</code></para>
        <para>Used in all standard <link linkend="app_tiers">blocks</link>.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.logDir">
      <term>cuba.logDir</term>
      <listitem>
        <para>Defines the location of the <link linkend="log_dir">log folder</link> for an <link linkend="app_tiers">application block</link>.</para>
        <para>Default value for the Middleware, Web Client and Web Portal blocks: <literal>${catalina.home}/logs</literal></para>
        <para>In case of a standard <structname>Tomcat</structname> deployment, this property points to the <filename>tomcat/logs</filename> folder.</para>
        <para>Default value for the Desktop Client block:  <literal>${<link linkend="cuba.desktop.home">cuba.desktop.home</link>}/logs</literal>. </para>
        <para>Interface: <code>GlobalConfig</code></para>
        <para>Used in all standard <link linkend="app_tiers">blocks</link>.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.mainMessagePack">
      <term>cuba.mainMessagePack</term>
      <listitem>
        <para>Defines the <link linkend="main_message_pack">main message pack</link> for an application block.</para>
        <para>The value may include a single pack or a list of packs separated with spaces.</para>
        <para>Used in all standard <link linkend="app_tiers">blocks</link>.</para>
        <para>Example:<programlisting>cuba.mainMessagePack = com.haulmont.cuba.web com.sample.sales.web</programlisting></para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.menuConfig">
      <term>cuba.menuConfig</term>
      <listitem>
        <para>Defines a set of  <link linkend="menu.xml">menu.xml</link> files. </para>
        <para>The property value should be a list of files separated with spaces.  <link linkend="resources">Resources</link>  interface rules apply to loading files.</para>
        <para>Used in the Web Client and Desktop Client blocks.</para>
        <para>Example:<programlisting>cuba.menuConfig = cuba-web-menu.xml web-menu.xml</programlisting></para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.metadataConfig">
      <term>cuba.metadataConfig</term>
      <listitem>
        <para>Defines a set of  <link linkend="metadata.xml">metadata.xml</link> files.</para>
        <para>The property value should be a list of files separated with spaces.  <link linkend="resources">Resources</link> interface rules apply to loading files.</para>
        <para>Used in the Middleware, Web Client and Desktop Client.</para>
        <para>Example:<programlisting>cuba.metadataConfig = cuba-metadata.xml metadata.xml</programlisting></para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.passwordEncryptionModule">
      <term>cuba.passwordEncryptionModule</term>
      <listitem>
        <para>Defines the name of the bean used for user password hashing.</para>
        <para>Default value: <literal>cuba_Sha1EncryptionModule</literal></para>
        <para>Used in all standard <link linkend="app_tiers">blocks</link>.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.passwordPolicyEnabled">
      <term>cuba.passwordPolicyEnabled</term>
      <listitem>
        <para>Enables password policy enforcement. If the property is set to <literal>true</literal>, all new user passwords will be checked according to the  <link linkend="cuba.passwordPolicyRegExp">cuba.passwordPolicyRegExp</link> property.</para>
        <para>Default value: <literal>false</literal></para>
        <para>Interface: <code>ClientConfig</code></para>
        <para>Used in the client blocks: Web Client, Web Portal, Desktop Client.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.passwordPolicyRegExp">
      <term>cuba.passwordPolicyRegExp</term>
      <listitem>
        <para>Defines a regular expression used by the password checking policy.</para>
        <para>Default value:</para>
        <para><prompt>((?=.*\\d)(?=.*\\p{javaLowerCase}) (?=.*\\p{javaUpperCase}).{6,20})</prompt></para>
        <para>The expression above ensures that password contains from 6 to 20 characters, uses numbers and Latin letters, contains at least one number, one lower case and one upper case letter. More information on regular expression syntax is available at <ulink url="https://en.wikipedia.org/wiki/Regular_expression">https://en.wikipedia.org/wiki/Regular_expression</ulink> and <ulink url="http://docs.oracle.com/javase/7/docs/api/java/util/regex/Pattern.html">http://docs.oracle.com/javase/6/docs/api/java/util/regex/Pattern.html</ulink>. </para>
        <para>Interface: <code>ClientConfig</code></para>
        <para>Used in the client level blocks: Web Client, Web Portal, Desktop Client.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.permissionConfig">
      <term>cuba.permissionConfig</term>
      <listitem>
        <para>Defines a set of  <link linkend="permissions.xml">permissions.xml</link> files. </para>
        <para>Used in the Web Client and Desktop Client blocks.</para>
        <para>Example:<programlisting>cuba.permissionConfig = cuba-web-permissions.xml web-permissions.xml</programlisting></para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.persistenceConfig">
      <term>cuba.persistenceConfig</term>
      <listitem>
        <para>Defines a set of  <link linkend="persistence.xml">persistence.xml</link> files.</para>
        <para>The property value should be a list of files separated with spaces.  <link linkend="resources">Resources</link> interface rules apply to loading files.</para>
        <para>Used in the Middleware, Web Client and Desktop Client.</para>
        <para>Example:<programlisting>cuba.persistenceConfig = cuba-persistence.xml persistence.xml</programlisting></para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.portal.anonymousUserLogin">
      <term>cuba.portal.anonymousUserLogin</term>
      <listitem>
        <para>Defines a user login that should be used for anonymous session in the Web Portal block. </para>
        <para>The user with the specified login should exist in the security subsystem and should have the required permissions. User password is not required, because anonymous portal sessions are created via the  <link linkend="login">loginTrusted()</link> method with the password defined in the  <link linkend="cuba.trustedClientPassword">cuba.trustedClientPassword</link> property.</para>
        <para>Interface:  <code>PortalConfig</code></para>
        <para>Used in the Web Portal block.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.randomServerPriority">
      <term>cuba.randomServerPriority</term>
      <listitem>
        <para>Enables random selection of a Middleware server to connect to in a cluster, to ensure uniform distribution of clients between servers.</para>
        <para>See also the <link linkend="cuba.connectionUrlList">cuba.connectionUrlList</link> property.</para>
        <para>Default value: <code>false</code></para>
        <para>Used in the Web Client, Web Portal, Desktop Client blocks.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.remotingSpringContextConfig">
      <term>cuba.remotingSpringContextConfig</term>
      <listitem>
        <para>Defines a set of  <link linkend="remoting-spring.xml">remoting-spring.xml</link> files in the Middleware block.</para>
        <para>The property value should be a list of files separated with spaces.  <link linkend="resources">Resources</link> interface rules apply to loading files.</para>
        <para>Used in the Middleware block.</para>
        <para>Example:<programlisting>cuba.remotingSpringContextConfig = cuba-remoting-spring.xml remoting-spring.xml</programlisting></para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.rest.productionMode">
      <term>cuba.rest.productionMode</term>
      <listitem>
        <para>Activates the  <link linkend="rest_api">REST API</link> production mode that does not return exception text to client.  </para>
        <para>Interface:  <code>RestConfig</code></para>
        <para>Used in the  <structname>Web Portal</structname> block.</para>
        <para>Default  value:  <literal>false</literal></para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.restApiUrl">
      <term>cuba.restApiUrl</term>
      <listitem>
        <para>URL to the application’s <link linkend="rest_api">REST API</link>.  </para>
        <para>Interface:  <code>GlobalConfig</code></para>
        <para>Can be used in all standard <link linkend="app_tiers">blocks</link>.</para>
        <para>Default value: <literal>http://localhost:8080/app-portal/api</literal></para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.schedulingActive">
      <term>cuba.schedulingActive</term>
      <listitem>
        <para>Enables the CUBA <link linkend="scheduled_tasks">scheduled tasks</link> mechanism.  </para>
        <para>Default value: <literal>false</literal></para>
        <para>Interface:  <code>ServerConfig</code></para>
        <para>Used in the Middleware block.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.springContextConfig">
      <term>cuba.springContextConfig</term>
      <listitem>
        <para>Defines a set of  <link linkend="spring.xml">spring.xml</link> files in an application block.</para>
        <para>The property value should be a list of file names separated with spaces.  <link linkend="resources">Resources</link> interface rules apply to loading files.</para>
        <para>Used in all standard <link linkend="app_tiers">blocks</link>.</para>
        <para>Example:<programlisting>cuba.springContextConfig = cuba-spring.xml spring.xml</programlisting></para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.supportEmail">
      <term>cuba.supportEmail</term>
      <listitem>
        <para>Specifies an email address to which exception reports from the default exception handler screen, as well as user messages from the <guimenu>Help</guimenu> &gt; <guimenu>Feedback</guimenu> screen will be sent.</para>
        <para><guibutton>Report</guibutton> button in the exception handler screen will be hidden, if this property is set to an empty string.</para>
        <para>In order to successfully send emails, the parameters described in  <xref linkend="email_sending_properties"/> must also be configured.</para>
        <para>Interface:  <code>WebConfig</code></para>
        <para>Used in the Web Client block.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.systemInfoScriptsEnabled">
      <term>cuba.systemInfoScriptsEnabled</term>
      <listitem>
        <para>Enables the display of SQL-scripts for creating / updating / retrieving an entity instance in the <guilabel>System Information</guilabel> window.</para>
        <para>Such scripts actually show the contents of the database rows that store the selected entity instance, regardless of security settings that may deny viewing of some attributes. That is why it is reasonable to revoke the <userinput>CUBA / Generic UI / System Information</userinput> specific <link linkend="permissions">permission</link> for all user  <link linkend="roles">roles</link> except the administrators, or set the  <code>cuba.systemInfoScriptsEnabled</code> to  <code>false</code> for the whole application.</para>
        <para>Default value:  <literal>true</literal></para>
        <para>Interface:   <code>ClientConfig</code></para>
        <para>Used in the Web Client and the Desktop Client blocks.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.tempDir">
      <term>cuba.tempDir</term>
      <listitem>
        <para>Defines the location of the <link linkend="temp_dir">temporary directory</link> for an <link linkend="app_tiers">application block</link>.</para>
        <para>The default value for the Middleware, Web Client, Web Portal blocks: <literal>${catalina.home}/temp/${<link linkend="cuba.webContextName">cuba.webContextName</link>}</literal></para>
        <para>In case of a standard <application>Tomcat</application> deployment, this points to a sub-folder with the name of the current web-app in the <filename>tomcat/temp</filename> folder, for example, <filename>tomcat/temp/app-core</filename>.</para>
        <para>The default value for the Desktop Client block:  <literal>${<link linkend="cuba.desktop.home">cuba.desktop.home</link>}/temp</literal>. </para>
        <para>Interface: <code>GlobalConfig</code></para>
        <para>Used in all standard <link linkend="app_tiers">blocks</link>.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.triggerFilesCheck">
      <term>cuba.triggerFilesCheck</term>
      <listitem>
        <para>Enables the processing of bean invocation trigger files.</para>
        <para>The trigger file is a file that is placed in the <filename>triggers</filename> subdirectory of the application block’s <link linkend="temp_dir">temporary directory</link>. The trigger file name consists of two parts separated with a period. The first part is the <link linkend="managed_beans">bean</link> name, the second part is the method name of the bean to invoke. For example: <code>cuba_Messages.clearCache</code>. The trigger files handler monitors the folder for new trigger files, invokes the appropriate methods and then removes the files.</para>
        <para>By default, the trigger files processing is configured in the <filename>cuba-web-spring.xml</filename> file and performed for the Web Client block only. At the project level, the processing for other modules can be performed by <link linkend="scheduled_tasks">periodically invoking</link> the <code>process()</code> method of the <code>cuba_TriggerFilesProcessor</code> bean.</para>
        <para>Default value: <literal>true</literal></para>
        <para>Used in blocks with the configured processing, default is Web Client.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.themeConfig">
      <term>cuba.themeConfig</term>
      <listitem>
        <para>Defines a set of <filename>*-theme.properties</filename> files that store   <link linkend="gui_themes">theme</link> variables,  such as default popup window dimensions and input field width.  </para>
        <para>The property takes a list of files separated by spaces. The files are loaded as defined by the  <link linkend="resources">Resources</link> interface.</para>
        <para><para>Default value for Web Client:  <literal>havana-theme.properties halo-theme.properties</literal></para>Default value for Desktop Client: <literal>nimbus-theme.properties</literal></para>
        <para>Used in the Web Client and Desktop Client block.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.trustedClientPassword">
      <term>cuba.trustedClientPassword</term>
      <listitem>
        <para>Defines password used by the <code>LoginService.loginTrusted()</code> method. The Middleware layer can authenticate users who connect via the trusted client <link linkend="app_tiers">block</link> without checking the user password. </para>
        <para>This property is used when user passwords are not stored in the database, while the client block performs the actual authentication itself. For example, by integrating with <application>Active Directory</application>.</para>
        <para>Interfaces:  <code>ServerConfig</code>, <code>WebConfig</code></para>
        <para>Used in blocks: Middleware, Web Client, Web Portal.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.trustedClientPermittedIpMask">
      <term>cuba.trustedClientPermittedIpMask</term>
      <listitem>
        <para>Defines IP address mask in form of regular expression, to which the invocation of the <code>LoginService.loginTrusted()</code> method is allowed.</para>
        <para>Default value: <literal>127\.0\.0\.1</literal></para>
        <para>Interfaces:  <code>ServerConfig</code>, <code>WebConfig</code></para>
        <para>Used in blocks: Middleware, Web Client, Web Portal.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.uniqueConstraintViolationPattern">
      <term>cuba.uniqueConstraintViolationPattern</term>
      <listitem>
        <para>A regular expression which is used to find out that the exception is caused by a database unique constraint violation. The first or second group of the expression must return the constraint name. For example:<programlisting>ERROR: duplicate key value violates unique constraint &quot;(.+)&quot;</programlisting></para>
        <para>The constraint name can be used to display a localized message that indicates what entity is concerned. For this the <link linkend="main_message_pack">main message pack</link> should contain keys equal to constraint names. For example:<programlisting>IDX_SEC_USER_UNIQ_LOGIN = A user with the same login already exists</programlisting></para>
        <para>This property allows to define a reaction to unique constraint violations depending on DBMS locale and version.</para>
        <para>Default value is returned by the <code>PersistenceManagerService.getUniqueConstraintViolationPattern()</code> method for the current DBMS.</para>
        <para>Used in all client blocks.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.useCurrentTxForConfigEntityLoad">
      <term>cuba.useCurrentTxForConfigEntityLoad</term>
      <listitem>
        <para>Enables using current transaction, if there is one at the moment, for loading entity instances via the <link linkend="config_interfaces">configuration interfaces</link>. This could have a positive impact on performance. Otherwise, a new transaction is always created and committed, and the detached instances are returned.</para>
        <para>Default value: <literal>false</literal></para>
        <para>Used in the Middleware block.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.useLocalServiceInvocation">
      <term>cuba.useLocalServiceInvocation</term>
      <listitem>
        <para>When set to <code>true</code>, and Tomcat <link linkend="fast_deployment">fast deployment</link> is used, the Web Client and Web Portal <link linkend="app_tiers">blocks</link> invoke the Middleware services locally, bypassing the network stack, which has a positive impact on system performance. This property should be set to false for all other deployment options.</para>
        <para>Default value: <literal>false</literal></para>
        <para>Used in the Web Client and Web Portal blocks.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.user.fullNamePattern">
      <term>cuba.user.fullNamePattern</term>
      <listitem>
        <para>Defines the full name pattern for user. </para>
        <para>Default value: <literal>{LL| }{F|. }{M|.}</literal></para>
        <para>The full name pattern can be formed from the user’s first, last and middle names. The following rules apply to the pattern:</para>
        <itemizedlist>
          <listitem>
            <para>The pattern parts are separated by <code>{}</code></para>
          </listitem>
          <listitem>
            <para>The pattern inside <code>{}</code> must contain one of the following characters followed by the <code>|</code> character without any spaces:</para>
            <para><code>LL</code> –  long form of user’s last name (Smith)</para>
            <para><code>L</code> – short form of user’s last name (S)</para>
            <para><code>FF</code> – long form of user’s first name (John)</para>
            <para><code>F</code> – short form of user’s first name (J)</para>
            <para><code>MM</code> – long form of user’s middle name (Paul)</para>
            <para><code>M</code> – short form of user’s middle name (P)</para>
          </listitem>
          <listitem>
            <para>The <code>|</code> character can be followed by any symbols including spaces.</para>
          </listitem>
        </itemizedlist>
        <para>Used in the Web Client and Desktop Client blocks.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.user.namePattern">
      <term>cuba.user.namePattern</term>
      <listitem>
        <para>Defines the display name pattern for the <code>User</code> entity. The display name is used in different places, including the upper right corner of the system’s main window.</para>
        <para>Default value: <code>{1} [{0}]</code></para>
        <para><code>{0}</code> is substituted with the <code>login</code> attribute, <code>{1}</code> – with the <code>name</code> attribute.</para>
        <para>Used in the Middleware, Web Client and Desktop Client blocks.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.userSessionExpirationTimeoutSec">
      <term>cuba.userSessionExpirationTimeoutSec</term>
      <listitem>
        <para>Defines the user session expiration timeout in seconds.</para>
        <para>Default value: <literal>1800</literal></para>
        <para>Interface: <code>ServerConfig</code></para>
        <para>Used in the Middleware block.</para>
        <tip>
          <para>It is recommended to use the same values for <property>cuba.userSessionExpirationTimeoutSec</property> and <link linkend="cuba.httpSessionExpirationTimeoutSec"> cuba.httpSessionExpirationTimeoutSec</link>. </para>
        </tip>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.userSessionProviderUrl">
      <term>cuba.userSessionProviderUrl</term>
      <listitem>
        <para>Defines the Middleware <link linkend="app_tiers">block</link> URL used for logging users in.</para>
        <para>This parameter should be set in additional middleware blocks that execute client requests, but do not share the user session cache. If there is no required session in the local cache at the start of the request, this block invokes the <code>LoginService.getSession()</code> method at the specified URL, and caches the retrieved session.</para>
        <para>Interface: <code>ServerConfig</code></para>
        <para>Used in the Middleware block.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.viewsConfig">
      <term>cuba.viewsConfig</term>
      <listitem>
        <para>Defines a list of the <link linkend="views.xml">views.xml</link> files, automatically deployed during startup of the application. See <xref linkend="views"/>.</para>
        <para>Used in all standard <link linkend="app_tiers">blocks</link>.</para>
        <para>Example:<programlisting>cuba.viewsConfig = cuba-views.xml reports-views.xml views.xml</programlisting></para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.webAppUrl">
      <term>cuba.webAppUrl</term>
      <listitem>
        <para>Defines URL of the Web Client application.</para>
        <para>In particular, used to generate external application <link linkend="link_to_screen">screen links</link>, as well as by the <code>ScreenHistorySupport</code> class.</para>
        <para>Interface:  <code>GlobalConfig</code></para>
        <para>Can be used in all standard <link linkend="app_tiers">blocks</link>.</para>
        <para>Default value: <literal>http://localhost:8080/app</literal></para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.windowConfig">
      <term>cuba.windowConfig</term>
      <listitem>
        <para>Defines a list of the <link linkend="screens.xml">screens.xml</link> files. </para>
        <para>The property value should contain a list of file names separated with spaces.  <link linkend="resources">Resources</link> interface rules apply to loading files.</para>
        <para>Used in the Web Client and Desktop Client blocks.</para>
        <para>Example:<programlisting>cuba.windowConfig = cuba-web-screens.xml web-screens.xml</programlisting></para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.web.appFoldersRefreshPeriodSec">
      <term>cuba.web.appFoldersRefreshPeriodSec</term>
      <listitem>
        <para>Defines <link linkend="folders_pane">application folders</link> refresh period in seconds.</para>
        <para>Default value: <literal>180</literal></para>
        <para>Interface: <code>WebConfig</code></para>
        <para>Used in the Web Client block.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.web.appWindowMode">
      <term>cuba.web.appWindowMode</term>
      <listitem>
        <para>Determines the initial mode for the main application window – tabbed or single screen (<code>TABBED</code> or <code>SINGLE</code> respectively). In single screen mode, screens opened in the <code>NEW_TAB</code> mode

will replace the current screen, instead of opening a new tab. This can be useful for simple applications and inexperienced users.</para>
        <para>The user is able to change the mode later, using the <guimenu>Help</guimenu> &gt; <guimenu>Settings</guimenu> screen.</para>
        <para>Default value: <literal>TABBED</literal></para>
        <para>Interface: <code>WebConfig</code></para>
        <para>Used in the Web Client block.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.web.foldersPaneDefaultWidth">
      <term>cuba.web.foldersPaneDefaultWidth</term>
      <listitem>
        <para>Defines default width (in pixels) for the <link linkend="folders_pane">folders panel</link>.</para>
        <para>Default value: <literal>200</literal></para>
        <para>Interface: <code>WebConfig</code></para>
        <para>Used in the Web Client block.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.web.foldersPaneEnabled">
      <term>cuba.web.foldersPaneEnabled</term>
      <listitem>
        <para>Enables the folders panel functionality.</para>
        <para>Default value: <literal>true</literal></para>
        <para>Interface: <code>WebConfig</code></para>
        <para>Used in the Web Client block.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.web.foldersPaneVisibleByDefault">
      <term>cuba.web.foldersPaneVisibleByDefault</term>
      <listitem>
        <para>Determines whether the <link linkend="folders_pane">folders panel</link> should be expanded by default.</para>
        <para>Default value: <literal>false</literal></para>
        <para>Interface: <code>WebConfig</code></para>
        <para>Used in the Web Client block.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.web.linkHandlerActions">
      <term>cuba.web.linkHandlerActions</term>
      <listitem>
        <para>Defines a list of URL commands handled by the <code>LinkHandler</code> bean. See <xref linkend="link_to_screen"/> for more information.</para>
        <para>The elements should be separated with the <code>|</code> character.</para>
        <para>Default value: <literal>open|o</literal></para>
        <para>Interface: <code>WebConfig</code></para>
        <para>Used in the Web Client block.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.web.loginDialogDefaultUser">
      <term>cuba.web.loginDialogDefaultUser</term>
      <listitem>
        <para>Defines default user name, which will be automatically populated in the login screen. This is very convenient during development. This property should be set to <code>&lt;disabled&gt;</code> value in production environment.</para>
        <para>Default value: <literal>admin</literal></para>
        <para>Interface: <code>WebConfig</code></para>
        <para>Used in the Web Client block.</para>
      </listitem>
    </varlistentry>
    <varlistentry>
      <term>cuba.web.loginDialogDefaultPassword</term>
      <listitem>
        <para>Defines default user password, which will be automatically populated in the login screen. This is very convenient during development. This property should be set to <code>&lt;disabled&gt;</code> value in production environment.</para>
        <para>Default value: <literal>admin</literal></para>
        <para>Interface: <code>WebConfig</code></para>
        <para>Used in the Web Client block.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.web.maxTabCount">
      <term>cuba.web.maxTabCount</term>
      <listitem>
        <para>Defines the maximum number of tabs that can be opened in the main application window. The value of <code>0</code> disables this limitation.</para>
        <para>Default value: <literal>7</literal></para>
        <para>Interface: <code>WebConfig</code></para>
        <para>Used in the Web Client block.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.web.rememberMeEnabled">
      <term>cuba.web.rememberMeEnabled</term>
      <listitem>
        <para>Enables displaying <guilabel>Remember Me</guilabel> checkbox in the standard login screen of the web client.</para>
        <para>Default value: <code>true</code></para>
        <para>Interface: <code>WebConfig</code></para>
        <para>Used in Web Client.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.web.resourcesRoot">
      <term>cuba.web.resourcesRoot</term>
      <listitem>
        <para>Sets a directory for loading files to display by <link linkend="gui_Embedded">Embedded</link> component. For example:<programlisting>cuba.web.resourcesRoot = ${cuba.confDir}/resources</programlisting></para>
        <para>Default value: <literal>null</literal></para>
        <para>Interface: <code>WebConfig</code></para>
        <para>Used in Web Client.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.web.showFolderIcons">
      <term>cuba.web.showFolderIcons</term>
      <listitem>
        <para>Enables the <link linkend="folders_pane">folders panel</link> icons. When enabled, the following application theme files are used:<itemizedlist>
            <listitem>
              <para><filename>icons/app-folder-small.png</filename> – for application folders.</para>
            </listitem>
            <listitem>
              <para><filename>icons/search-folder-small.png</filename> – for search folders.</para>
            </listitem>
            <listitem>
              <para><filename>icons/set-small.png</filename> – for record sets.</para>
            </listitem>
          </itemizedlist></para>
        <para>Default value: <literal>false</literal></para>
        <para>Interface: <code>WebConfig</code></para>
        <para>Used in the Web Client block.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.web.theme">
      <term>cuba.web.theme</term>
      <listitem>
        <para>Defines the name of the   <link linkend="web_theme">theme</link> used as default for the web client. See
            also <link linkend="cuba.themeConfig">cuba.themeConfig</link>.</para>
        <para>Default value: <literal>havana</literal></para>
        <para>Interface: <code>WebConfig</code></para>
        <para>Used in the Web Client block.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.web.useFontIcons">
      <term>cuba.web.useFontIcons</term>
      <listitem>
        <para>If this property is enabled for Halo <link linkend="web_theme">theme</link>,  <ulink url="http://fortawesome.github.io/Font-Awesome">Font Awesome</ulink> glyphs will be used for standard actions and platform screens instead of images.</para>
        <para>The correspondence between the name in the  <link linkend="gui_attr_basic_icon">icon</link> attribute of a visual component or action and font element is defined in the <filename>halo-theme.properties</filename> file of the platform. Keys with <code>cuba.web.icons</code> prefixes correspond to icon names, and their values - to <code>com.vaadin.server.FontAwesome</code> enumeration constants. For example, a font element for the standard  <code>create</code> action is defined as follows:<programlisting>cuba.web.icons.create.png = FILE_O</programlisting></para>
        <para>Default value: <literal>true</literal></para>
        <para>Interface: <code>WebConfig</code></para>
        <para>Used in the Web Client block.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.web.useInverseHeader">
      <term>cuba.web.useInverseHeader</term>
      <listitem>
        <para>Controls the web client application header for Halo  <link linkend="web_theme">theme</link> and its <link linkend="web_theme_extension">inheritors</link>. If  <code>true</code>, the header will be dark (inverse), if  <code>false</code> - the header takes the colour of the main application background.</para>
        <para>This property is ignored in case  <programlisting>$v-support-inverse-menu: false;</programlisting>property is set in the application theme. This makes sense for a dark theme, if the user has the option to choose between a light and a dark theme. In this case, the header will be inverse for the light theme, and the same as the main background in the dark theme.</para>
        <para>Default value: <literal>true</literal></para>
        <para>Interface: <code>WebConfig</code></para>
        <para>Used in the Web Client block.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.web.useLightHeader">
      <term>cuba.web.useLightHeader</term>
      <listitem>
        <para>Enables the compact version of the upper part of the main application window – logo, menu bar, user name and logout button will be placed in single line. When disabled, <code>AppWindow.createTitleLayout()</code> creates additional area at the top.</para>
        <para>Default value: <literal>true</literal></para>
        <para>Interface: <code>WebConfig</code></para>
        <para>Used in the Web Client block.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.web.viewFileExtensions">
      <term>cuba.web.viewFileExtensions</term>
      <listitem>
        <para>Defines a list of file extensions displayed in the browser when <link linkend="file_download">downloading the file</link> using <code>ExportDisplay.show()</code>. The <code>|</code> character should be used to separate the list items.</para>
        <para>Default value: <literal>htm|html|jpg|png|jpeg|pdf</literal></para>
        <para>Interface: <code>WebConfig</code></para>
        <para>Used in the Web Client block.</para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.webContextName">
      <term>cuba.webContextName</term>
      <listitem>
        <para>Defines the web application context name. It is usually equivalent to the name of the directory or WAR-file containing this <link linkend="app_tiers">application block</link>.</para>
        <para>Interface: <code>GlobalConfig</code></para>
        <para>Used in blocks: Middleware, Web Client, Web Portal.</para>
        <para>For example, for the Middleware block, located in <filename>tomcat/webapps/app-core</filename> and available at <filename>http://somehost:8080/app-core</filename>, the property should be set to the following value:<programlisting>cuba.webContextName = app-core</programlisting></para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.webHostName">
      <term>cuba.webHostName</term>
      <listitem>
        <para>Defines the host name of the machine, on which this <link linkend="app_tiers">application block</link> is running.</para>
        <para>Default value: <literal>localhost</literal></para>
        <para>Interface: <code>GlobalConfig</code></para>
        <para>Used in blocks: Middleware, Web Client, Web Portal.</para>
        <para>For example, for the Middleware block, available at <filename>http://somehost:8080/app-core</filename>, the property should be set to the following value:<programlisting>cuba.webHostName = somehost</programlisting></para>
      </listitem>
    </varlistentry>
    <varlistentry id="cuba.webPort">
      <term>cuba.webPort</term>
      <listitem>
        <para>Defines the port, on which this <link linkend="app_tiers">application block</link> is running.</para>
        <para>Default value: <literal>8080</literal></para>
        <para>Interface: <code>GlobalConfig</code></para>
        <para>Used in blocks: Middleware, Web Client, Web Portal.</para>
        <para>For example, for the Middleware block, available at <filename>http://somehost:8080/app-core</filename>, this property should be set to the following value:<programlisting>cuba.webPort = 8080</programlisting></para>
      </listitem>
    </varlistentry>
  </variablelist>
</appendix>
